(function() {
    'use strict';

    angular.module('app')
        .factory('ShowModel', ShowModel);

    // ShowModel.$inject = [];

    function ShowModel() {

        var model = {

        };
        return model;
    }
})();
